# Marchen 執行計画書 — パンマルシェ完成に向けて

## 現状分析サマリー

### 既に完成している機能 ✅

| 機能 | 状態 | 詳細 |
|---|---|---|
| 認証・RBAC | ✅ 完成 | NextAuth + JWT + Credentials / ADMIN・ORGANIZER・VENDOR |
| イベントCRUD | ✅ 完成 | 作成・編集・公開・締切・CSV出力 |
| 出店申請フロー | ✅ 完成 | VENDOR申請・ORGANIZER承認/拒否・取り下げ |
| 監査ログ | ✅ 完成 | 全意思決定を記録 |
| Google Calendar連携 | ✅ 完成 | OAuth2 + FreeBusy取得 + 空き時間提案 |
| 面談スロット管理 | ✅ 完成 | 手動作成 + 自然言語入力（日本語対応） |
| DBスキーマ | ✅ 完成 | 15モデル全て定義済み（UI未実装のものあり） |

### DBモデルは存在するがUI・ロジック未実装 🔶

| モデル | 用途 |
|---|---|
| Budget | 予算管理 |
| EquipmentBooking | アメニティ・備品 |
| StaffingPlan | 人員計画 |
| VolunteerInvite | ボランティア招聘 |
| SalesRecord | 売上記録 |
| WeatherAlert | 天気アラート |
| Notification | 通知（メール・Instagram DM） |

### 完全に未実装 ❌

| 機能 | 説明 |
|---|---|
| チャットベースUI | 全ワークフローをチャットで完結 |
| AI日程最適化 | ベンダー空き情報からベスト開催日を計算 |
| ベンダー側の日程選択UI | 面談候補日をベンダーに提示→選択 |
| アメニティカタログ・購入 | 備品一覧・発注フロー |
| AI人員算出 | 規模から必要人数を自動計算 |
| イベント告知ページ（公開） | ボランティア・一般向け公開ページ |
| 仕様書自動生成 | デザイナー向けコピペ用資料 |
| ボランティア管理 | メール登録・リマインド送信 |
| 天気API連携 | 前日アラート |
| PayPay/QR決済連携 | 当日売上入力 |
| 会計・活動報告書生成 | 地方公共団体向けレポート |
| 感想シェア | 事後フィードバック |

---

## 実装フェーズ（優先度順）

### Phase 1: チャットベース・ワークフローエンジン（コア）
**なぜ最初か:** ユーザーの構想では「全体としてchatでパンマルシェを開くのが完了するアプリ」。これが基盤。

### Phase 2: ベンダー面談・日程調整の完成
**なぜ2番目か:** ワークフローの最初のステップ。既存のMeetingSlot・GoogleCalendar基盤を活用。

### Phase 3: アメニティ・備品管理
**なぜ3番目か:** 日程確定後の次ステップ。

### Phase 4: 人員計画・ボランティア管理
**なぜ4番目か:** 開催準備段階の機能。

### Phase 5: イベント告知・仕様書生成
**なぜ5番目か:** 募集・広報段階。

### Phase 6: 当日運営（決済・天気・売上）
**なぜ6番目か:** 当日〜事後の機能。

### Phase 7: レポート・感想シェア
**なぜ最後か:** 事後処理。

---

## Phase 1: チャットベース・ワークフローエンジン

### 必要な変更

#### 1-1. スキーマ追加
```prisma
model ChatThread {
  id        String   @id @default(cuid())
  eventId   String   @map("event_id")
  event     Event    @relation(fields: [eventId], references: [id], onDelete: Cascade)
  messages  ChatMessage[]
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")
  @@map("chat_threads")
}

model ChatMessage {
  id        String   @id @default(cuid())
  threadId  String   @map("thread_id")
  thread    ChatThread @relation(fields: [threadId], references: [id], onDelete: Cascade)
  role      String   // "user" | "assistant" | "system"
  content   String
  metadata  Json?    // tool calls, action results
  createdAt DateTime @default(now()) @map("created_at")
  @@map("chat_messages")
}
```

#### 1-2. AI統合 (OpenAI or Anthropic API)
- `src/lib/ai.ts` — AI client + ワークフロー用システムプロンプト
- `src/app/actions/chat.ts` — チャットサーバーアクション
- ワークフローステップを「ツール」としてAIに公開

#### 1-3. チャットUI
- `src/components/Chat.tsx` — メッセージ表示 + 入力
- `src/components/ChatSidebar.tsx` — ワークフロー進捗表示
- `src/app/organizer/events/[id]/chat/page.tsx`

---

## Phase 2: ベンダー面談・日程調整の完成

### 2-1. ベンダー向け面談候補選択UI（公開リンク）
- `src/app/meeting/[token]/page.tsx` — トークンベースの公開ページ
- ベンダーがログイン不要で面談日を選べる
- スマホ対応カレンダーUI

### 2-2. 独自カレンダーコンポーネント
- `src/components/AvailabilityCalendar.tsx` — 各ベンダーの空き状況を視覚表示
- 自然言語入力 → カレンダー反映の強化

### 2-3. AI日程最適化エンジン
- `src/lib/schedule-optimizer.ts`
- 全ベンダーの空き情報を集約 → 最大参加可能日を算出
- 「この日なら開催できます！」をチャットで提案

### 2-4. 自動通知（承認フロー）
- 候補日決定 → ベンダーに通知メール送信
- ベンダー承認 → ステータス更新 + オーガナイザーに通知
- Instagramしかないベンダー向けのDM文面自動生成

### スキーマ追加
```prisma
model MeetingInvite {
  id            String   @id @default(cuid())
  meetingSlotId String?  @map("meeting_slot_id")
  vendorTargetId String  @map("vendor_target_id")
  token         String   @unique @default(cuid())
  expiresAt     DateTime @map("expires_at")
  respondedAt   DateTime? @map("responded_at")
  createdAt     DateTime @default(now()) @map("created_at")
  @@map("meeting_invites")
}
```

---

## Phase 3: アメニティ・備品管理

### 3-1. 備品カタログ
```prisma
model EquipmentCatalog {
  id          String   @id @default(cuid())
  name        String
  category    String
  source      EquipmentSource
  priceYen    Int      @map("price_yen")
  vendorName  String?  @map("vendor_name") // 貸出業者名
  description String?
  imageUrl    String?  @map("image_url")
  ownerId     String?  @map("owner_id") // 自治体所有の場合
  @@map("equipment_catalog")
}
```

### 3-2. UI
- `src/app/organizer/events/[id]/equipment/page.tsx` — 備品選択画面
- カタログから選択 → 数量指定 → EquipmentBooking作成
- 「購入ボタン」で発注（MVP: メール送信 or 外部リンク）

---

## Phase 4: 人員計画・ボランティア管理

### 4-1. AI人員算出
- `src/lib/staffing-calculator.ts`
- 入力: 出店数、会場面積、想定来場者数、時間帯
- 出力: 役割別必要人数（受付、誘導、設営、撤収など）

### 4-2. ボランティア管理UI
- `src/app/organizer/events/[id]/volunteers/page.tsx`
- メールアドレス入力 → 招待メール送信
- リマインドメール自動送信（前日・当日朝）
- ステータス管理（招待済・確認済・辞退）

### 4-3. メール送信基盤
- `src/lib/mailer.ts` — Resend or Nodemailer
- テンプレートベースのメール（招待・リマインド・通知）

---

## Phase 5: イベント告知・仕様書生成

### 5-1. 公開イベントページ
- `src/app/events/[id]/public/page.tsx` — 一般・ボランティア向け
- 出店者一覧、日時場所、ボランティア募集フォーム

### 5-2. 仕様書自動生成
- `src/app/actions/spec-generator.ts`
- チャットで「告知用の仕様書を作って」→ Markdown/PDF生成
- デザイナーへのコピペ用フォーマット（イベント名・日時・場所・出店者・雰囲気）
- 簡易ポスターテンプレート

---

## Phase 6: 当日運営

### 6-1. 売上入力UI
- `src/app/organizer/events/[id]/sales/page.tsx`
- 出店者別売上入力（PayPay QRコード表示はMVPではリンク）
- SalesRecordへの保存

### 6-2. 天気アラート
- `src/lib/weather.ts` — OpenWeatherMap API連携
- 前日自動チェック → 雨予報なら通知
- `src/app/organizer/events/[id]/weather/page.tsx`

### 6-3. PayPay連携（将来）
- MVP: 売上手動入力 + PayPayリンク表示
- 将来: PayPay API統合

---

## Phase 7: レポート・感想シェア

### 7-1. 会計・活動報告書生成
- `src/app/actions/report-generator.ts`
- チャットで「会計報告書を作って」→ 予算・売上・出店者情報を集約
- 地方公共団体向けフォーマット（PDF/Excel）

### 7-2. 感想シェア
```prisma
model Feedback {
  id        String   @id @default(cuid())
  eventId   String   @map("event_id")
  event     Event    @relation(fields: [eventId], references: [id])
  authorId  String?  @map("author_id")
  authorName String? @map("author_name")
  content   String
  rating    Int?     // 1-5
  isPublic  Boolean  @default(false) @map("is_public")
  createdAt DateTime @default(now()) @map("created_at")
  @@map("feedbacks")
}
```
- 出店者・ボランティア・来場者から感想収集
- 公開ページでシェア

---

## 技術スタック追加

| 追加パッケージ | 用途 |
|---|---|
| `ai` + `@ai-sdk/openai` or `@ai-sdk/anthropic` | Vercel AI SDK（チャット・ツール呼び出し） |
| `resend` | メール送信 |
| `@react-email/components` | メールテンプレート |
| `date-fns` | 日付操作 |
| `react-day-picker` | カレンダーUI |
| `jspdf` or `@react-pdf/renderer` | PDF生成 |

---

## 環境変数追加

```env
# AI
OPENAI_API_KEY=           # or ANTHROPIC_API_KEY
# Email
RESEND_API_KEY=
EMAIL_FROM=noreply@marchen.local
# Weather
OPENWEATHERMAP_API_KEY=
# PayPay (将来)
PAYPAY_API_KEY=
PAYPAY_API_SECRET=
```
